runtime: python311

env: standard
entrypoint: gunicorn -b :$PORT app:app

env_variables:
  FLASK_ENV: "production"

handlers:
- url: /.*
  script: auto

automatic_scaling:
  min_instances: 1
  max_instances: 10
